## DATE module
1) By default date is printed with less than 5hr 30min less than the current time
2) Use toString() method to get correct time

## copying of varibles and objects
1) ...objname  is used to make a copy of object
2) But in nested object it fail to copy it create a new reference for nested objects
3) we use a function "structuredClone()" function to make a copy of nested object